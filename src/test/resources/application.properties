# Test overrides — datasource is provided by Testcontainers @ServiceConnection
spring.jpa.hibernate.ddl-auto=create-drop
app.jwt.secret=test-secret-for-testing-purposes-must-be-at-least-32-chars
app.jwt.expiry-seconds=3600
app.auth.username=testuser
app.auth.password=testpass
app.cors.allowed-origins=http://localhost:3000

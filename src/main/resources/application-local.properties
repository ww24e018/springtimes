# Local development defaults — activate with -Dspring.profiles.active=local
# These values are NOT suitable for production
app.jwt.secret=local-dev-secret-not-for-production-at-least-32-chars
app.auth.username=admin
app.auth.password=changeme
